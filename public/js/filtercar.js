const modeles = [];
const moteurs = [];
const options = [];

var modelCar = modeles.filter(function(word){
    if (modelCar == Volkswagen Passat) {
        return 
    }
})
function filter(type, value){
    
}